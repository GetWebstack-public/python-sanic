apiVersion: v1
kind: Service
metadata:
  name: python-sanic
  namespace: getwebstack
  labels:
    app: python-sanic
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: python-sanic
